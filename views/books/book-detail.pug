extends ../layout

block content

  h1 Book Details

  p Title: #{book.title}
  p Author: #{book.author}
  p Genre: #{book.genre}
  p Year: #{book.year}

  p
    a(class='button' href=`/books/${book.id}/edit`) Edit Book
  p
    a(class='button' href='/books') Home
