extends ../layout

block content
  h1= title

  include errors

  form(action="/books", method="post")
    include form
    input(type="submit" value="Update Book")
  
  form(action=`/books/${book.id}/delete`, method="post")
    p
      a(class='button' href='/books') Cancel
    p
      input#deleteBtn(type="submit" value="Delete Book")
